// Change './' to '/your-repo-name/' when deploying to a GitHub project page.
// Example: base: '/konstantinoupoli/'
// Leave as './' for root deployments (custom domain or user/org page).
export default {
  base: './',
}
